import Todo from './basic_todo.js';

// The logic of the app, is going to be:

    // Create projects arrays,
    // Create a todo,(Just use the => [new Todo] <=<)
    // Add it to the current project(Added method into class)
    // 



const one = new Todo("memo");
one.setPriority("high");
one.pushArray();





